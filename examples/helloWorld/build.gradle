plugins {
    id 'java-library'
    id 'io.fungrim.github.blackan.gradle' version '0.0.2-SNAPSHOT'
}

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    implementation libs.common
    implementation libs.slf4j.api
    implementation "io.github.fungrim.blackan.extension:jetty:0.0.2-SNAPSHOT"
    implementation "io.github.fungrim.blackan.extension:jackson:0.0.2-SNAPSHOT"
    // test classes
    testImplementation libs.junit.jupiter
    testImplementation libs.mockito
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    // lombok
    compileOnly libs.lombok
    annotationProcessor libs.lombok
    testCompileOnly libs.lombok
    testAnnotationProcessor libs.lombok
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

tasks.named('test', Test) {
    useJUnitPlatform()
}