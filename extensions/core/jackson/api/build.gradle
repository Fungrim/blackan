plugins {
    id 'java-library'
    id 'maven-publish'
}

dependencies {
    api libs.jackson.databind
}

publishing {
    publications {
        mavenJava {
            artifactId = 'blackan-jackson-extension-api'
        }
    }
}
